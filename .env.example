# Database Configuration
# For local development with PostgreSQL running on Docker:
DATABASE_URL="postgresql://insurance_user:insurance_pass@localhost:5432/insurance_engine?schema=public"

# For production or cloud databases, use your connection string:
# DATABASE_URL="postgresql://user:password@host:port/database?schema=public"

# Agent Configuration (for single-agent mode)
DEFAULT_AGENT_ID="1"

# Scoring Configuration
# Lead scoring weights (optional, can be overridden in code)
# SCORING_WEIGHT_ACTIVITY=10
# SCORING_WEIGHT_INTENT_HOT=50
# SCORING_WEIGHT_INTENT_WARM=30
# SCORING_WEIGHT_INTENT_COLD=10
